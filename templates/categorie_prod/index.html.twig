{% extends 'base.html.twig' %}

{% block title %}CategorieProd index{% endblock %}

{% block body %}
    <h1>CategorieProd index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Categorie_prod</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_prod in categorie_prods %}
            <tr>
                <td>{{ categorie_prod.id }}</td>
                <td>{{ categorie_prod.categorieProd }}</td>
                <td>
                    <a href="{{ path('app_categorie_prod_show', {'id': categorie_prod.id}) }}">show</a>
                    <a href="{{ path('app_categorie_prod_edit', {'id': categorie_prod.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_prod_new') }}">Create new</a>
{% endblock %}
