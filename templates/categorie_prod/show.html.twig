{% extends 'gestionProd/baseback1.html.twig' %}

{% block body %}
    <div class="container">
        <div class="card-header">
            <h1 class="title">Consulter la catégorie : {{ categorie_prod.categorieProd }}</h1>
        </div>
        <div class="card-body">
            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ categorie_prod.id }}</td>
                    </tr>
                    <tr>
                        <th>Categorie_prod</th>
                        <td>{{ categorie_prod.categorieProd }}</td>
                    </tr>
                </tbody>
            </table>

            <div class="d-flex">
            {{ include('categorie_prod/_delete_form.html.twig') }}
                <a href="{{ path('app_categorie_prod_edit', {'id': categorie_prod.id}) }}" class="btn btn-success mb-0.9 custom-margin-between-buttons">Modifier</a>
              
                <a href="{{ path('app_categorie_prod_index') }}" class="btn btn-secondary ">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
