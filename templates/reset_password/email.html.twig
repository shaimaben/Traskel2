<h1>Hi!</h1>

<p>To reset your password, please visit the following link</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) | replace({'localhost': '127.0.0.1:8000'}) }}">
    {{ url('app_reset_password', {token: resetToken.token}) | replace({'localhost': '127.0.0.1:8000'}) }}
</a>
<p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cheers!</p>
