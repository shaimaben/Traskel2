{% extends 'base.html.twig' %}

{% block title %} edit Profil {% endblock %}

{% block content %}
    <div class="grix xs4 container">
        <div class="col-xs4">
            <h1>Modification du profil </h1>
            {{ form_start(form) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn blue">Modifier le profil</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
